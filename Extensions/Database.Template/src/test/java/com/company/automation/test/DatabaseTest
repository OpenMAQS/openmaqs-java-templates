package com.company.automation.test;

import io.github.maqs.database.BaseDatabaseTest;
import io.github.maqs.utilities.helper.exceptions.ExecutionFailedException;
import io.github.maqs.utilities.helper.exceptions.TimeoutException;
import org.testng.Assert;
import org.testng.annotations.Test;

public class DatabaseTest extends BaseDatabaseTest {

  /**
   * Get record using stored procedure.
   */
  @Test
  public void GetRecordTestNUnit() {
    // TODO: Update database connection configuration and add test code
    // List<Object> result = this.getDatabaseDriver().query("SELECT * FROM information_schema.tables").toArray());
    // Assert.assertEquals(1, result.size(), "Expected 1 state abbreviation to be returned.");

    this.getLogger().logMessage("Dummy test");
  }
}